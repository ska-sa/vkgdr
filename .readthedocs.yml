version: 2
sphinx:
  configuration: doc/conf.py
  fail_on_warning: true
python:
  install:
    - requirements: requirements-readthedocs.txt
build:
  os: ubuntu-24.04
  tools:
    python: "3.10"
  jobs:
    pre_build:
      - cd doc && doxygen
